<h1>All the Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.task, task_path(task) %> (<%= task.description %>) (<%= task.rating %>)</li>
  <% end %>
</ul>

<p><%= link_to "Add a Task", new_task_path %></p>
